package diagrams.instagram
{
	import flash.display.MovieClip;
	import flash.events.Event;
	
	public class DiagramLines extends DiagramPreveiw
	{
		
		public function DiagramLines(diagramValues:InstagramData,W:Number,H:Number,titleData:InstagramTitles)
		{
			super(diagramValues,W,H,titleData);
		}
	}
}